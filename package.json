{
  "name": "cmed",
  "version": "1.0.0",
  "description": "API server that will control all the http calles between the frontend and the DB",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --poll ./src/index.ts",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/index.js"
  },
  "author": "Jennifer Gonzalez Alzate",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.0",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
